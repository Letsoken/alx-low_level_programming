#include "main.h"

/**
 * _isdigit - Identifies digits
 * @c: First operand
 *
 * Description: Determines if character is digit
 * Return: Always equals 1 or 0
 */

int _isupper(int c)
{
	if (c >=0'A' && c <= 'Z')
		return (1);
	else
		return (0);

}

